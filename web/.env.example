# Copy to .env.local for local dev (Vite picks up .env.local automatically).
# In production (Vercel), set this as a project Environment Variable instead.

# Where the backend API is reachable. Local dev matches `npm run dev` in /server.
VITE_API_ORIGIN=http://localhost:8080
